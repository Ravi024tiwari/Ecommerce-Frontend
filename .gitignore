# Dependencies
node_modules/

# Production Build (Vite/React build folder)
dist/
build/

# Environment Variables
.env
.env.local
.env.development
.env.test
.env.production

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS Files
.DS_Store
Thumbs.db

# Editor Directories
.idea
.vscode
*.swp
*.swo

# Testing
coverage/
```

---

### ⚠️ Zaroori Note:
Agar aapne **pehle se hi** Git initialize kar diya tha aur galti se `.env` ya `node_modules` commit kar diye the, toh `.gitignore` add karne ke baad bhi wo track hote rahenge.

Unhe hatane ke liye terminal mein yeh command chalayein (dono folders mein alag-alag):

```bash
git rm -r --cached .
git add .
git commit -m "Fixed gitignore"